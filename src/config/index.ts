// ? Global default configuration items

// Home address
export const HOME_URL: string = "/";

// login page address
export const LOGIN_URL: string = "/login";

// default theme color
export const DEFAULT_PRIMARY: string = "#3AE3B9";

// Routing whitelist address (must be in a locally existing routing staticRouter.ts)
export const ROUTER_WHITE_LIST: string[] = ["/500", "/404", "/403", "/loading"];

// AMAP_MAP_KEY
export const AMAP_MAP_KEY: string = "";

// BAIDU_MAP_KEY
export const BAIDU_MAP_KEY: string = "";

export const project_name: string = "test";

export const h5game_name: string = "h5game";
